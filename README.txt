		--English--
The Helper AI extension is powered by AI. It allows you to condense your texts while retaining key details and the main idea.
The text summarization is driven by the Qwen 2.5 14B model.

To install the extension, you need to enable Developer mode in the Extensions section, and then click the "Load unpacked" button (the extension must be located in a folder in a convenient place for you).

To activate it, you need to obtain an API key for this neural network:
First, open the HuggingFace website and go to the Qwen section (make sure you are logged in).Click on your profile avatar and select "Access Tokens" below.
Create a new token, choose "Fine-grained" as the type, and under the "Interface" permissions, check "Make calls to Inference Providers".
Paste the received code into the extension settings and save it (the key will persist after a restart).
To condense a text, highlight it, right-click, and select "Summarize with Helper AI". 
Then, open the extension—the text will already be processed or in the process of being handled by the AI. (As a result, the AI has partially transitioned into a chatbot, so if you ask it a question, it will give you an answer.)

Android Installation: Download a browser that supports extensions, enable developer mode (if necessary), and import the .zip file. Open the HuggingFace website and go to the Qwen section (you must be logged in). Click on your profile avatar and select "Access Tokens" below. Create a new token, choose "Fine-grained" as the token type, and under the Interface section in permissions, check "Make calls to Inference Providers". Paste the generated token into the extension settings (using the Paste button) and save it. (As a result, the AI has partially converted into a chatbot and will answer any questions you ask.)
		--Russian--
Расширение Helper AI построено с помощью ИИ. С помощью него вы можете сжимать ваши тексты, оставляя подробности и главную мысль. Модель, которая сжимает тексты, — Qwen 2.5 14B.
Установка для PC: Для установки в разделе расширения нужно включить режим разработчика, а затем нажать кнопку "Загрузить распакованное расширение" (Расширение должно находиться в папке в удобном вам месте).
Активация:Для активации вам нужно получить API-ключ для данной нейросети:Для начала открываете сайт HuggingFace в разделе Qwen (нужно быть залогиненным).Жмем на аватарку профиля и ниже выбираем "Access Tokens".Создаем новый токен, в типе выбираем "Fine-grained", а в разрешениях в разделе Interface — "Make calls to Inference Providers".Полученный код вставляем в настройки расширения и сохраняем его (при перезагрузке ключ не теряется).Для сжатия текста нужно выделить его, нажать ПКМ и "Summarize with Helper AI", а затем, открыв расширение, текст уже будет обработан/обрабатываться нейросетью. (По итогам нейросеть перешла частично в чат бота и если задать вопрос она вам на него ответит)

Установка для Android: Скачайте браузер который поддерживает расширения,включите режми разработчика (если без него не получается) и импортируйте .zip. Откройте сайт HuggingFace в разделе Qwen (нужно быть залогиненым). Жмем на аватарку профиля и ниже выбираем "Access Tokens".Создаем новый токен, в типе выбираем "Fine-grained", а в разрешениях в разделе Interface — "Make calls to Inference Providers". Полученный код вставляем в настройки расширения (кнопка Paste) и сохраняем его. (По итогам нейросеть перешла частично в чат бота и если задать вопрос она вам на него ответит)

									
						
												by Frontalt
